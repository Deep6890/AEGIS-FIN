# AEGIS-FIN LogicEngine — Python dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: Python 3.14 on Windows — use --prefer-binary to avoid C++ compiler errors.
#   pip install -r requirements.txt --prefer-binary

# ── Core data / numerics ──────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.50

# ── Database backend (required when STORE_BACKEND=supabase) ──────────────────
# supabase pulls in postgrest, gotrue, realtime, storage3, httpx etc.
# Pin storage3 below 0.12.0 to avoid the pyiceberg C-extension build on Windows.
supabase>=2.0.0
storage3<0.12.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
