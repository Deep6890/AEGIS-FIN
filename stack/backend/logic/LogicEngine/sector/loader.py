import pandas as pd
from sector_engine import mainExe

if __name__ == "__main__":
    results = mainExe()
    print("\nAll results collected successfully!")
    # Sector dataframes are now available in the `results` dictionary

